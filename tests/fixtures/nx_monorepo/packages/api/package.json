{
  "name": "api",
  "version": "1.0.0",
  "description": "API package for the monorepo",
  "main": "src/index.js",
  "scripts": {
    "build": "nx build",
    "test": "nx test"
  },
  "dependencies": {
    "express": "^4.18.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^18.0.0"
  }
}